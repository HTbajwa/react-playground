//ITs when you send fucction as a prop then the other one decides what to show
function render({render}){
    <h1>This the render component</h1>
}
import React from 'react'

const RenderProps = () => {
  return (
    <div>
      
    </div>
  )
}

export default RenderProps
